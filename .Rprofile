source("renv/activate.R")

if (interactive()) {

  suppressPackageStartupMessages(suppressWarnings({
    library(devtools) # always the first one to use messages!
    usethis::ui_done("Package {usethis::ui_value('devtools')} attached.")

    library(checkmate)
    usethis::ui_done("Package {usethis::ui_value('checkmate')} attached.")

    library(testthat)
    usethis::ui_done("Package {usethis::ui_value('testthat')} attached.")
  }))
}
